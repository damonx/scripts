apiVersion: v1
kind: ConfigMap
metadata:
  name: global-database-config-v4
  namespace: default
data:
  database.properties: |
    database.url=https://dbserver.somewhere.else.com:3306
    database.password=damonx
